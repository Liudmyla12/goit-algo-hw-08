# goit-algo-hw-08

Домашнє завдання до теми **“Купи, або піраміди”**.

У цьому репозиторії реалізовано завдання з використанням **мін-купи** (`heapq`).

---

## 📌 Структура репозиторію

```text
goit-algo-hw-08/
├── task_1.py      # Мінімальні витрати з’єднання кабелів (heapq)
├── task_2.py      # (optional) merge_k_lists через мін-купу
├── README.md
└── .gitignore
```

---

## ✅ Завдання 1 — Мінімальні витрати з’єднання кабелів (heapq)

### Суть задачі

Є кілька мережевих кабелів різної довжини. Потрібно з’єднувати їх **по два** так, щоб **загальна вартість** була мінімальною.

- Вартість одного з’єднання = **сума довжин двох кабелів**
- Загальна вартість = **сума всіх з’єднань**

### Рішення

Оптимальна стратегія: **завжди з’єднувати два найкоротші кабелі**.
Це класична задача, що вирішується **мін-купою** (`heapq`).

### ▶️ Запуск (Windows)

```bash
py .\task_1.py
```

Або передати довжини кабелів вручну:

```bash
py .\task_1.py 4 3 2 6
```

Очікувано програма виведе список кабелів та мінімальну загальну вартість.

---

## ⭐ Optional — merge_k_lists (k-way merge через мін-купу)

### Суть

Є `k` відсортованих списків. Потрібно об’єднати їх у **один** відсортований список, ефективно використовуючи **мін-купу**.

### ▶️ Запуск (Windows)

```bash
py .\task_2.py
```

Очікуваний приклад:

```
Відсортований список: [1, 1, 2, 3, 4, 4, 5, 6]
```

---

## 🧰 Вимоги

- Python 3.x (у мене тестувалось на Python 3.12)
- Будь-який термінал (PowerShell / Terminal) та IDE (VS Code)
